{{HENK_FILE_HEADER}}
# Generated by `henk` — hand-edits will be overwritten on `henk update`.
#
# Traefik file-provider config. Used by:
#   - M3: TLS material (mkcert wildcard cert is registered here).
#   - M6: Host-mode projects (per-project routers/services pointing at
#         host.docker.internal).
#
# In M2 this file is intentionally minimal — Docker-mode routing comes
# from container labels via the Docker provider. Empty `http`/`tls`
# sections must be omitted (Traefik 3.x rejects them as malformed),
# so we leave the file effectively empty until M3 lands.
