<!-- managed by henk — see https://github.com/fivespark/henk -->
<!-- Generated by `henk` — hand-edits will be overwritten on `henk up`. -->
<!--
  The chrome every henk error page shares. `templates.rs` renders a page body
  into {{BODY}} — the pages themselves are body fragments, so the styling below
  is written once instead of once per page.
-->
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>{{TITLE}} — henk</title>

<style>
  :root {
    color-scheme: light dark;
    --bg: #fbfaf8;
    --fg: #1c1b19;
    --muted: #6b6862;
    --rule: #e3e0da;
    --accent: #b45309;
    --code-bg: #f0eeea;
  }

  @media (prefers-color-scheme: dark) {
    :root {
      --bg: #171614;
      --fg: #f2f0ec;
      --muted: #9c988f;
      --rule: #2e2c28;
      --accent: #f0b429;
      --code-bg: #232120;
    }
  }

  body {
    margin: 0;
    padding: 3rem 1.5rem;
    background: var(--bg);
    color: var(--fg);
    font: 16px/1.6 ui-sans-serif, -apple-system, "Segoe UI", sans-serif;
    display: flex;
    justify-content: center;
  }

  main {
    max-width: 42rem;
    width: 100%;
  }

  .eyebrow {
    margin: 0 0 0.5rem;
    color: var(--accent);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  h1 {
    margin: 0 0 0.75rem;
    font-size: 1.75rem;
    line-height: 1.25;
  }

  .lede {
    margin: 0 0 2rem;
    color: var(--muted);
  }

  h2 {
    margin: 0 0 1rem;
    font-size: 0.95rem;
    font-weight: 600;
  }

  ol {
    margin: 0 0 2rem;
    padding-left: 1.25rem;
  }

  li {
    margin-bottom: 0.85rem;
  }

  li p {
    margin: 0.25rem 0 0;
    color: var(--muted);
    font-size: 0.9rem;
  }

  code {
    padding: 0.1em 0.35em;
    border-radius: 4px;
    background: var(--code-bg);
    font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
    font-size: 0.875em;
  }

  .fix {
    padding-top: 1.5rem;
    border-top: 1px solid var(--rule);
    color: var(--muted);
    font-size: 0.9rem;
  }

  .fix strong {
    color: var(--fg);
  }
</style>

{{BODY}}
