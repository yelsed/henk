//! Terminal-UI components.
//!
//! `wizard` (M5) — `henk init` step-by-step wizard with consent prompts
//! (lives directly in `cli/init.rs`, no separate module needed today).
//! `dashboard` (M8) — live route + container view via ratatui.

pub mod dashboard;
